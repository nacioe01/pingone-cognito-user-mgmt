# ===========================================
# ENVIRONMENT VARIABLES TEMPLATE
# ===========================================
# Copy this file to:
#   - client/.env (for frontend)
#   - server/.env (for backend)
# ===========================================

# ===========================================
# Cognito Configuration (Required)
# ===========================================

# Your Cognito User Pool ID (e.g., us-east-1_XXXXXXXXX)
VITE_COGNITO_USER_POOL_ID=us-east-1_XXXXXXXXX

# Your Cognito App Client ID (from App integration > App clients)
VITE_COGNITO_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx

# Your Cognito Domain (without https://)
# Example: your-domain.auth.us-east-1.amazoncognito.com
VITE_COGNITO_DOMAIN=your-domain.auth.us-east-1.amazoncognito.com

# AWS Region (e.g., us-east-1)
VITE_COGNITO_REGION=us-east-1

# ===========================================
# OAuth Configuration (Required)
# ===========================================

# Redirect URI after successful login (must match Cognito App Client settings)
VITE_REDIRECT_URI=http://localhost:5173/callback

# Logout redirect URI
VITE_LOGOUT_URI=http://localhost:5173

# Identity Provider name as configured in Cognito (default: Ping)
VITE_IDENTITY_PROVIDER=Ping

# ===========================================
# Server Configuration (for server/.env only)
# ===========================================

# Server port
SERVER_PORT=3001

# Cognito App Client Secret (IMPORTANT: Keep this on server only!)
COGNITO_CLIENT_SECRET=your-client-secret-here

# Same as VITE values but for server-side use
COGNITO_USER_POOL_ID=us-east-1_XXXXXXXXX
COGNITO_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx
COGNITO_DOMAIN=your-domain.auth.us-east-1.amazoncognito.com
COGNITO_REGION=us-east-1
REDIRECT_URI=http://localhost:5173/callback

# ===========================================
# Ping Identity Reference (configured in AWS Console)
# ===========================================
# Note: Ping is configured as a SAML or OIDC IdP in Cognito User Pool
# These are for reference only - actual config is in AWS Console:
# 
# For OIDC IdP:
#   - PING_ISSUER_URL=https://auth.pingone.com/{environment-id}/as
#   - PING_CLIENT_ID=your-ping-client-id
#   - PING_CLIENT_SECRET=your-ping-client-secret
#
# For SAML IdP:
#   - PING_METADATA_URL=https://auth.pingone.com/{environment-id}/saml20/metadata/{application-id}
